function rvrString(str) {
  if (str === "")
    return "";
  else
    return rvrString(str.substr(1)) + str.charAt(0);
}
reverseString("paul");

function reverseString(str) {
  return (str === '') ? '' : reverseString(str.substr(1)) + str.charAt(0);
}
